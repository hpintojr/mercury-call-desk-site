# Copy to .env.local (never commit .env.local). Add the same keys in Vercel > Project > Settings > Environment Variables.

# Canonical site URL (sitemap/robots/OG)
NEXT_PUBLIC_SITE_URL=https://mercurycalldesk.com

# ---- Sales team dial script (/dial-script) ----
# One shared password for agents. Required in Production (and Preview if you want
# to test there). Changing it signs everyone out. Copy in src/content/dial-script.ts.
DIAL_SCRIPT_PASSWORD=

# ---- Sulus CRM (crm.sulus.ai) - lead delivery (/contact and landing-page callback forms) ----
# Preferred: a workflow "Inbound Webhook" trigger URL (+ optional signing secret → X-Webhook-Signature).
CRM_WEBHOOK_URL=
CRM_WEBHOOK_SECRET=
# Landing-page "call me in 60 seconds" form → dedicated "Ad Callback -> AI Call" workflow (falls back to the URL above if unset).
CRM_CALLBACK_WEBHOOK_URL=
CRM_CALLBACK_WEBHOOK_SECRET=
# Legacy fallback: LeadConnector-compatible API upsert (Sulus CRM does not expose this; kept for GHL-compatible targets).
#   CRM_LOCATION_ID  = the id in your CRM URL: https://crm.sulus.ai/location/<THIS>
CRM_LOCATION_ID=6R986ILIQydGAU4T1l74
CRM_API_TOKEN=
# CRM_API_BASE=https://services.leadconnectorhq.com
# CRM_API_VERSION=2021-07-28

# ---- Tracking (all optional; each tag loads only when its ID is set) ----
# Preferred: one GTM container that holds GA4, Meta Pixel and the Google Ads tag.
NEXT_PUBLIC_GTM_ID=
# Or load tags directly without GTM:
NEXT_PUBLIC_GA4_ID=
NEXT_PUBLIC_META_PIXEL_ID=
NEXT_PUBLIC_GOOGLE_ADS_ID=
